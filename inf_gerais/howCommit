@byDesso

-- para configurar o GitHub
    git init
    git add README.md
    git commit -m "first commit"
    git remote add origin "https://github.com/marcosrigueiral/yesodvazio"
    git push -u origin master

-- para commitar no GitHub (desso)

    git config user.name "Anderson Morais"
    git config user.email dessomorais@hotmail.com
*   git add src/Handler/Entrada.hs                                       (por exemplo)
    git commit -m "Comentar o commit de forma bem compreensível"
    git push origin master
--  fornecer login e senha do GitHub (login: dessoGitHub)
    
*   caminho completo, por exemplo:
    git add /home/ubuntu/workspace/GAM/src/Handler/Home.hs

*   em tudo, por exemplo:
    git add .

-- para commitar no GitHub (Marcos)
    git config user.name "Marcos Rigueiral"
    git config user.email marcos.rigueiral@hotmail.com
*   git add .
    git commit -m "Comentar o commit de forma bem compreensível"
    git push origin master
--  fornecer login e senha do GitHub (login: marcosrigueiral)

-- para commitar no GitHub (Gabs)
    git config user.name "Gabriel Fonseca"
    git config user.email gabrielfonseca.coutinho@outlook.com
*   git add .
    git commit -m "Comentar o commit de forma bem compreensível"
    git push origin master
--  fornecer login e senha do GitHub (login: supersonicgabs)